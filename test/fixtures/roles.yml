# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

<% roles = [
    "Toastmaster", "Invocator", "Speaker", "Topicsmaster",
    "General Evaluator", "Evaluator", "Ah Counter", "Vote Counter",
    "Grammarian", "Time Keeper" ] %>
<% roles.each_index do |role_index| %>
role_<%= role_index %>:
  name: <%= roles[role_index] %>
<% end %>
